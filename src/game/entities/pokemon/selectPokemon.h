#include "../../../../utils/base.h"
#include "../structs.h"


 //carga los nombres desde el txt y los guarda en un arreglo que se recorre
void showPokemonNames();
 //función que muestra los pokemones y evalúa el número ingresado
int selectPokemon(Pokemon &character);
//función que configura del pokemon cpu de forma automática
int setCpuPokemon(Pokemon &character);